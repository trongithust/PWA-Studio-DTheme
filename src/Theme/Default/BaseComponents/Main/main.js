import React from 'react';
import { default as VeniaMain } from '@magento/venia-ui/lib/components/Main';

const Main = (props) => {
    return (
        <VeniaMain {...props}/>
    );
};

export default Main;
