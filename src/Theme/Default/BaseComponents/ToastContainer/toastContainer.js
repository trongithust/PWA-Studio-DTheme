import React from 'react';
import { default as VeniaToastContainer } from '@magento/venia-ui/lib/components/ToastContainer';

const ToastContainer = () => {
    return (
        <VeniaToastContainer />
    );
};

export default ToastContainer;
