import React from 'react';
import { default as VeniaCategory } from '@magento/venia-ui/lib/RootComponents/Category';

const Category = (props) => {
    return (
        <VeniaCategory {...props}/>
    );
};

export default Category;
