import React from 'react';
import { default as VeniaNotFound } from '@magento/venia-ui/lib/RootComponents/NotFound';

const NotFound = (props) => {
    return (
        <VeniaNotFound {...props}/>
    );
};

export default NotFound;
